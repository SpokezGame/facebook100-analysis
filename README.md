# Lancement
Ce projet est composé d'un jupyter-notebook, commencez par initialiser votre environnement python (explication sans conda) :
```bash
python3 -m venv net
source net/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

Pour lancer le notebook :
```bash
jupyter-notebook
```
